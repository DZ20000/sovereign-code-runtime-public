# Changelog

## Unreleased - source-preview preparation

- Publish original project material under Apache-2.0 with separate upstream notices and a dependency-license inventory.
- Reject empty, whitespace, oversized, and recognizable example Gateway Bearer values at each configuration entry point. Leave the environment example non-operational and document secure local generation.
- Require a genuine Git source root and checked-out commit for Runtime Host and release provenance. Document the source-archive limitation instead of inventing a commit or borrowing an ancestor repository.
- Pin targeted dependency remediations and upgrade the test runner to the patched Vitest 4 line. Preserve full-audit findings and document residual build-tool risk separately from runtime reachability.
- Replace only Electron Packager's legacy extract-zip edge with the pinned Electron-maintained native extractor and add real-consumer containment regressions.
- Retain genuine tunnel failure evidence without manufacturing a failure at the short startup-grace deadline; correct recovery-fixture poll generations and stale documentation/installer static guards.
- Add source-preview/security/contribution documentation and a least-privilege Windows source-check workflow.

This section is not a release, installation record, signed-binary attestation, or claim that every platform and native interaction has been accepted. No release date or public binary download is announced here.
